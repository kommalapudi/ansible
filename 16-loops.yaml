- name: loops practice
  hosts: local
  connection: local
  become: true
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "{{ item }}" # item is reserverd keyword in ansible for loops
    loop:
      - chaitanya
      - hari
      - klinkaaras